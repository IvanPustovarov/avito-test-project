const refreshCommentsReducer = (state, action) => {
  switch (action.type) {
    case "REFRESH_COMMENTS":
      return state;
    default:
      return state;
  }
};
export default refreshCommentsReducer;
